import React from 'react';

/**
 * ChildishDrawingElement - A component to add childish drawing elements to the UI
 * (Currently disabled to remove all decorative elements from the app)
 */
const ChildishDrawingElement = () => {
  // Return null to render nothing - this effectively removes all decorative elements
  return null;
};

export default ChildishDrawingElement;
